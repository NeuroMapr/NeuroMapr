{
  "name": "neuromapr-backend",
  "version": "1.0.0",
  "private": true,
  "description": "NeuroMapr Raindrop Backend - AI-powered concept mapping",
  "scripts": {
    "build": "shx rm -rf dist && tsc -b",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:connections": "node test-vultr-connections.js"
  },
  "dependencies": {
    "@liquidmetal-ai/raindrop-framework": "^0.9.1",
    "@modelcontextprotocol/sdk": "^1",
    "zod": "^3"
  },
  "devDependencies": {
    "shx": "^0.4.0",
    "ts-node": "^10",
    "typescript": "^5.0.4",
    "typescript-eslint": "^8.7.0",
    "vitest": "^3.1.3"
  }
}